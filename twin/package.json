{
  "name": "gargantua-twin",
  "version": "1.0.0",
  "private": true,
  "description": "AI twin backend for amalshaji.in — Cloudflare Worker calling the Anthropic API.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "login": "wrangler login",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
